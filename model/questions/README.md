# Questions

One file per question, written against `meta/core/question-schema.md`. A question is worded as visitors ask it, its answer says where the answer lies, and its `## Rests on` names the entities that hold it, so the chat can match a visitor's words to it and answer from what it rests on.
