# Concepts

One file per concept, written against `meta/core/concept-schema.md`. A concept names the domain it belongs to and points at the concepts it hangs off; what points back at it is read, never written twice.
